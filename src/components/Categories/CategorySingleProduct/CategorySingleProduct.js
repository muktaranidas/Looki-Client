import React from "react";
import { useLoaderData } from "react-router-dom";

const CategorySingleProduct = () => {
  const categorySingleProduct = useLoaderData();

  return (
    <div>
      <h1>hi</h1>
    </div>
  );
};

export default CategorySingleProduct;
